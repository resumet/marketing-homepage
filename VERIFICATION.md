# 검증 결과

- npm run lint: 통과
- npm run build: 통과 (Next.js 16.3.5)
- npm test: Playwright Chrome 5개 통과
  - 상품군 4개, 이미지 로딩, 앵커 이동
  - 375px 모바일 메뉴, 가로 넘침 없음
  - 잘못된 비밀번호 거부, 0000 로그인, 상품/회사 정보 편집, 로그아웃
  - 비인증 및 다른 origin의 이미지 업로드 요청 거부
  - 데스크톱 라이트/다크 화면, 가로 넘침 없음
- 로컬 운영 빌드 Lighthouse 모바일: 성능 80, 접근성 100, 권장사항 100, SEO 100
- LCP 4.4초, CLS 0. 전체 한국어 폰트 대신 필요한 글자 묶음을 로드하여 초기 결과 LCP 12.5초에서 개선.
- Lighthouse는 JSON 보고서를 생성했으나 Windows 임시 Chrome 프로필 정리에서 권한 오류가 발생할 수 있음.

## 외부 연결 후 필요한 검증

Supabase 프로젝트와 자격증명이 아직 없으므로 실제 DB 저장, Storage 업로드, 분산 로그인 제한은 실서비스에서 검증하지 않았습니다. 연결 후 SQL을 실행하고 관리자에서 이미지 업로드 및 저장 → 홈페이지 새로고침 → 재접속 시 지속성을 확인해야 합니다.

GitHub 업로드와 Vercel 배포도 사용자가 추후 연결하기로 하여 수행하지 않았습니다.
